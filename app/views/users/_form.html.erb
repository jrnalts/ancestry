<%= simple_form_for(user, local: true) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>
      <ul>
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :name, label: "Name" %>
  </div>
  
  <div class="field">
      <%= f.input :parent, input_html: {value: params[:user_id]}, label: "Parent ID" %>
    <% if params[:user_id] %>
    <% else %>
      <%# <%= f.hidden :parent, value: params[:user_id] %>
    <% end %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
